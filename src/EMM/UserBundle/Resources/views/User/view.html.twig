{% extends 'layout.html.twig' %}

{% block body %}
    {{ parent() }}
    <div class="container well">
        <div class="col-md-9">
            <h2>{{ user.firstname }} {{ user.lastname }}</h2>
            <dl>
                <dt>{{ user.username }}</dt>
            </dl>
        </div>
    </div>
{% endblock %}